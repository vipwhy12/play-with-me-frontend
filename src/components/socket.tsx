import io, { Socket } from 'socket.io-client';

// const socket: Socket = io('ws://localhost:8080/socket');
// const socket = io('ws://localhost:8080/socket', {
//   transports: ['websocket'],
// });

// export default socket;
//
